# Example environment variables for local development and deployment
# Copy this file to .env or configure these in your Supabase Function / Deployment settings.

# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=public-anon-key-here
# Vite (frontend) environment variables
# For Vite dev/build, prefix client-accessible variables with VITE_
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=public-anon-key-here
# Optional: service role keys (keep secret, set in server-side function env only)
SUPABASE_SERVICE_ROLE=
SERVICE_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Mapbox
# Preferred: set MAPBOX_TOKEN for the public Mapbox token to use in the get-mapbox-token function
MAPBOX_TOKEN=
# Legacy fallback (if still used): MAPBOX_PUBLIC_TOKEN=

# Third-party keys
RESEND_API_KEY=

# Notes:
# - Never commit real secret keys to the repository. Use your deployment platform's secret management
#   (Supabase Functions environment variables, Vercel/Netlify/AWS secrets, etc.).
# - For Supabase Functions, set these env vars in the function's settings in the Supabase dashboard.
